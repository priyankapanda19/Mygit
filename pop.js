// let numbers = [1,2,3]

// numbers.pop()

// console.log(numbers);

// it removes the last element of the array by default

// IT WILL REMOVE THE LAST INDEX IN THE ARRAY 

// ================ get the removed item which has been removed =============

let numbers = [1,2,3]

let lastItem = numbers.pop() // this variable stores the last item which is 3 on index 2

console.log(numbers);
console.log(lastItem);
